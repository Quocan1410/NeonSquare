# Supabase DB (server-side, sensitive – do not commit)
SUPABASE_DB_PASSWORD=<SUPABASE_DB_PASSWORD>

# Supabase JS client (public, OK to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=<PUBLIC_SUPABASE_ANON_KEY>
